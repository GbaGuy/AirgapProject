Network Tools has been deployed.

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

{{- if not .Values.cronjob.enabled }}

To exec into the network tools pod:

  kubectl exec -it deploy/{{ include "networktools.fullname" . }} -n {{ .Release.Namespace }} -- bash

Available tools include: curl, dig, nslookup, ping, traceroute,
tcpdump, netcat, iperf3, mtr, ip, ss, nmap, and more.

{{- if .Values.scripts.enabled }}

Custom scripts are mounted at /scripts/.
{{- end }}

{{- else }}

CronJob is configured with schedule: {{ .Values.cronjob.schedule }}
{{- end }}
